[pytest]
DJANGO_SETTINGS_MODULE = StallManagement.settings
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    --reuse-db
    -v
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
